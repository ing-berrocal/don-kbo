pipeline {
    agent any
    stages {
        stage('Stage1') {
            steps {
                sh 'node --version'
            }
        }
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}